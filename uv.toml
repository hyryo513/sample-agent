# uv configuration (project-level placeholder)
# Replace values below with real registry / lockfile settings when available.

[project]
name = "sample-agent"
python = "3.13"

[uv]
# Example options you might configure for uv
registry = "pypi"
lockfile = "uv.lock"

[dev-dependencies]
# dev dependencies that uv should install in a dev env
- "pytest"
- "ruff"
- "black"
- "langgraph"
